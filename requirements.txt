# 基金投资助手 — 依赖列表
# ============================
# 当前版本使用纯标准库 + 内置 dataclasses，无需额外安装依赖。
# 接入真实数据源时可能需要：

# requests>=2.31.0        # HTTP 请求（接入真实 API/RSS）
# feedparser>=6.0.10      # RSS 解析
# pandas>=2.1.0           # 数据处理与回测
# akshare>=1.12.0         # A 股/基金数据接口（备选）
# schedule>=1.2.0         # 定时任务（备选方案）
